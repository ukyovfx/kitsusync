# Agent start here

Read `CURRENT-STATE.md` for the accepted default-branch state verified for this repository.

Put active task plans in `plans/active/` and completed plans in `plans/archive/`.

Use the existing project documentation for architecture and operator context, especially `README.md`, `CONTRIBUTING.md`, `docs/`, and `docs/notes/`. Verify technical claims against the actual code, tests, configuration, and CI before relying on them.
