# v0.4.4 release candidate

## Goal

Complete the v0.4.4 release candidate in PR #159 and reach an evidence-backed merge/release decision.

## Scope

- PR: #159 `Release v0.4.4 candidate: notification UX and connection management`
- Branch: `codex/v0.4.4-notification-card`
- Current PR head verified at: `b80ceee2358eb47669118c3702a7090da4622d2c`

## Status

Active RC work. The PR is open and draft. It is not accepted default-branch state.

## Verified completed checks from the PR evidence

- `go test ./src/... -count=1 -timeout=240s` PASS
- `go vet ./src/...` PASS
- `gofmt` and `git diff --check` PASS
- `docker compose config -q` PASS
- Docker build PASS
- staged secret-pattern scan PASS

## Remaining gates

- Controlled Discord E2E in the authorized disposable test Guild/channel
- Real notification-card verification
- Human Discord visual approval
- Refresh/reconcile the PR with current `master` as needed before merge and re-run any validation invalidated by that reconciliation
- Merge, tag, and release publication after the gates pass

## Deferred

- Connection Map

## Blockers

The remaining release gates require controlled Discord/runtime verification and human visual approval. Do not claim completion from code/tests alone.

## Next action

Run the controlled Discord E2E and real notification-card verification in the authorized test environment, capture the result in the PR/task state, then obtain human visual approval before merge/release.

## Relevant paths / evidence

- `AGENTS.md`
- `docs/agent/CURRENT-STATE.md`
- PR #159
- relevant source/tests/CI and release documentation referenced by the PR

## Write-back rule

Update this file only when verified durable task status, blockers, verification results, or the next action changes. Move it to `plans/archive/` after the release task is durably complete.
